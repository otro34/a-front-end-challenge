import styled from 'styled-components'

import { Toolbar as ToolbarMui  } from '@material-ui/core'

export const FilterContainer = styled.div`
    flex-grow: 1;
`

export const StyledToolbarMui = styled(ToolbarMui)`
    background-color: #1565c0;
`